info:
  name: detail invoice
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/invoices/{{id}}"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  variables:
    - name: id
      value: inv_002

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
