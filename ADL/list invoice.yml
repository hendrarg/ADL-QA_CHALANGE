info:
  name: list invoice
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/invoices"
  auth:
    type: bearer
    token: "{{access_token}}"

runtime:
  variables:
    - name: data
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
