info:
  name: unbiled invoice
  type: http
  seq: 7

http:
  method: GET
  url: "{{URL}}/invoices/unbilled/summary"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
